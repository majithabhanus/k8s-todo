1️⃣ What Helm really is (clear mental model)

Think like this:

    Helm = Kubernetes YAML + Go templating + values.yaml


Helm does NOT deploy apps magically.
It only does text substitution and then runs:

    kubectl apply -f rendered-yaml

So if Kubernetes YAML is wrong → Helm also fails.



2️⃣ Helm folder structure (must-know)

    todo-chart/
    ├── Chart.yaml        # chart metadata
    ├── values.yaml       # configuration inputs
    ├── templates/        # Kubernetes YAML with logic
    │   ├── deployment.yaml
    │   ├── service.yaml
    │   └── _helpers.tpl
    └── charts/           # dependencies (optional)


    📌 Rule:

    values.yaml = data

    templates = logic

    Chart.yaml = identity


3️⃣ Helm syntax basics (VERY IMPORTANT)

Helm uses Go template syntax.

🔹 Print a value

   image: {{ .Values.backend.image.repository }}

   👉 Means:

    .Values → values.yaml

    backend.image.repository → nested keys


  🔹 If condition
  
    {{- if .Values.frontend.enabled }}
    apiVersion: v1
    kind: Service
    {{- end }}


    ✔ Creates resource only if enabled
    ❌ No YAML indentation inside Helm blocks

🔹 Default value

   replicas: {{ .Values.backend.replicas | default 1 }}


   If not present → uses 1


 🔹 Loop (range)

    env:
    {{- range .Values.backend.env }}
    - name: {{ .name }}
        value: "{{ .value }}"
    {{- end }}


  values.yaml:
  
    backend:
    env:
      - name: DB_HOST
        value: mysql

4️⃣ The most dangerous Helm mistake ⚠️

❌ This causes crashes:

   value: {{ .Values.dbPort }}
  

   If dbPort: 3306 → Kubernetes expects string , not int

   ✅ Correct:
   
    value: "{{ .Values.dbPort }}"

    👉 Rule:
    📌 env.value must ALWAYS be string


5️⃣ Helm behavior (how Helm thinks)
    🧠 Helm lifecycle
    Install

    helm install todo-app .


    Renders templates

    Sends YAML to Kubernetes

    Saves release state in cluster   
    

**Upgrade**

    helm upgrade todo-app .


    Re-renders templates

    Diffs old vs new

    Applies only changes

**Rollback**

    helm rollback todo-app 1


  🚀 This is why companies love Helm. 


9️⃣ How Helm is used in real companies
    Dev
    helm install app . -f values-dev.yaml

    Staging
    helm upgrade app . -f values-staging.yaml

    Prod
    helm upgrade app . -f values-prod.yaml


Same chart. Different behavior.  
  
  
🔟 Helm vs pure Kubernetes (truth)
| Kubernetes YAML    | Helm        |
| ------------------ | ----------- |
| Static             | Dynamic     |
| Copy-paste         | Reusable    |
| No rollback        | Rollback    |
| Env-specific files | values.yaml |
| Manual edits       | Controlled  |


👉 Helm is not easier at first
👉 Helm is powerful after understanding syntax



1️⃣1️⃣ Golden rules to remember ⭐

✔ values.yaml = configuration only
✔ templates = no hardcoded values
✔ secrets = NOT in values.yaml
✔ env.value = always string
✔ selector labels = must match
✔ test with: 

    helm template .


 Real-world analogy 🍔
 
| Helm concept | Real-world    |
| ------------ | ------------- |
| Chart        | Burger recipe |
| Release      | One burger    |
| values.yaml  | Ingredients   |
| Release name | Order number  |


helm install <RELEASE_NAME> <CHART>



Helm Chart ≠ Release

They are two different things.

1️⃣ Helm Chart (what it is)

Chart = template + logic

It is:

a folder

reusable

environment-agnostic

written once

Example:

nginx-chart/
├── Chart.yaml
├── values.yaml
└── templates/


👉 This is like source code.




